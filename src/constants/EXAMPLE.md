# Constants Directory

Use this directory for reusable, project-wide constants.

Examples:

- Render defaults (background color, near/far plane values)
- Shared tuning values (movement speed, animation durations)
- Domain constants that should not be duplicated across files
